import { HighlightQuotesDirective } from './highlight-quotes.directive';

describe('HighlightQuotesDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightQuotesDirective();
    expect(directive).toBeTruthy();
  });
});
