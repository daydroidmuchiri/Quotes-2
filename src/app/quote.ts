export class Quote {
    showInfo: boolean;
      constructor(public id: number, public name: string, public publishDate)
                  {
                    this.showInfo = false;
                  }
  }

